# Crypto Trading API Client

Проект для работы с API криптобирж Bybit и Hyperliquid на Python.
Включает REST API, WebSocket для real-time данных и арбитражный мониторинг.

## Возможности

- ✅ Получение рыночных данных (тикеры, свечи, стакан)
- ✅ Управление ордерами (создание, отмена, просмотр)
- ✅ Проверка баланса
- ✅ **WebSocket подключения в реальном времени**
  - Последние сделки (trades)
  - Стакан заявок (orderbook)
  - Свечи (kline)
  - Тикеры (24h статистика)
- ✅ Поддержка testnet и mainnet
- ✅ Логирование операций
- ✅ Примеры использования

## Установка

### 1. Клонировать репозиторий или скачать файлы

### 2. Создать виртуальное окружение (рекомендуется)

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установить зависимости

```bash
pip install -r requirements.txt
```

### 4. Настроить API ключи

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Получите API ключи на Bybit:
   - Testnet: https://testnet.bybit.com/app/user/api-management
   - Mainnet: https://www.bybit.com/app/user/api-management

3. Добавьте ключи в `.env` файл

**⚠️ ВАЖНО:** Начните с testnet для безопасного тестирования!

## Структура проекта

```
PythonApi/
├── bybit_client.py          # REST API клиент
├── websocket_client.py      # WebSocket клиент для real-time данных
├── config.py                # Конфигурация проекта
├── requirements.txt         # Зависимости Python
├── .env.example             # Пример файла с переменными окружения
├── .env                     # Ваши API ключи (не коммитится!)
├── .gitignore              # Игнорируемые файлы
├── README.md               # Документация
├── examples/               # Примеры использования
│   ├── market_data.py      # Получение рыночных данных (REST)
│   ├── trading.py          # Торговые операции (REST)
│   ├── websocket_trades.py # WebSocket: последние сделки
│   └── websocket_advanced.py # WebSocket: расширенный пример
└── utils/                  # Утилиты
    ├── logger.py           # Настройка логирования
    └── encoding.py         # Исправление кодировки Windows
```

## Примеры использования

### Получение рыночных данных (без API ключей)

```bash
python examples/market_data.py
```

Этот пример показывает:
- Время сервера
- Текущую цену BTC/USDT
- Исторические свечи
- Стакан заявок

### Торговые операции (требуются API ключи)

```bash
python examples/trading.py
```

Этот пример показывает:
- Проверку баланса
- Размещение ордеров
- Просмотр открытых ордеров
- Отмену ордеров

### WebSocket: Последние сделки в реальном времени

**Простой мониторинг (5 основных пар):**
```bash
python examples/websocket_simple.py
```

**Все 35 торговых пар с автоматической сводкой:**
```bash
python examples/websocket_all_pairs.py
```

**Детальный вывод:**
```bash
python examples/websocket_trades.py
```

Возможности:
- 🔥 Мониторинг 35 торговых пар одновременно
- 📊 Автоматическая сводка каждые 60 секунд
- 📈 Индикаторы активности (🔥🔥🔥, 🔥🔥, 🔥, 💤)
- 💹 Цена, объем, направление каждой сделки
- ⏱️ Временные метки в реальном времени

### WebSocket: Расширенный мониторинг

```bash
python examples/websocket_advanced.py
```

Одновременный мониторинг нескольких потоков:
- Последние сделки (trades)
- Тикеры с 24h статистикой
- Стакан заявок в реальном времени
- Автоматическая сводка каждые 30 секунд

### Использование в коде

```python
from bybit_client import BybitClient

# Инициализация клиента
client = BybitClient()

# Получение тикера
ticker = client.get_tickers(category="spot", symbol="BTCUSDT")
print(ticker)

# Размещение лимитного ордера
order = client.place_order(
    category="spot",
    symbol="BTCUSDT",
    side="Buy",
    orderType="Limit",
    qty="0.001",
    price="30000",
    timeInForce="GTC"
)
print(order)

# Проверка баланса
balance = client.get_wallet_balance(accountType="UNIFIED")
print(balance)
```

## Основные методы API

### Рыночные данные
- `get_server_time()` - время сервера
- `get_tickers(category, symbol)` - информация о тикерах
- `get_kline(category, symbol, interval, limit)` - свечи
- `get_orderbook(category, symbol, limit)` - стакан заявок

### Аккаунт
- `get_wallet_balance(accountType)` - баланс кошелька

### Торговля
- `place_order(...)` - разместить ордер
- `get_open_orders(category, symbol)` - открытые ордера
- `cancel_order(category, symbol, orderId)` - отменить ордер

## Безопасность

⚠️ **ВАЖНЫЕ ПРАВИЛА:**

1. **НИКОГДА** не коммитьте `.env` файл с API ключами в git
2. Начинайте тестирование на **testnet**, а не на mainnet
3. Используйте API ключи только с необходимыми правами
4. Ограничьте IP адреса для API ключей в настройках Bybit
5. Храните API ключи в безопасности

## Документация Bybit API

- Официальная документация: https://bybit-exchange.github.io/docs/v5/intro
- pybit библиотека: https://github.com/bybit-exchange/pybit

## Поддержка

При возникновении вопросов или проблем:
1. Проверьте документацию Bybit API
2. Убедитесь, что используете актуальные API ключи
3. Проверьте логи в `logs/bybit_api.log`

## Лицензия

Этот проект создан в образовательных целях. Используйте на свой риск.
